{
  "name": "@supperajan/avatar",
  "version": "0.1.0",
  "private": true,
  "description": "3D avatar animation state machine, behavior configs, and scene utilities",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./behaviors": "./src/behaviors/index.ts",
    "./scene": "./src/scene/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "@react-three/fiber": "^8.0.0",
    "three": "^0.160.0"
  },
  "dependencies": {
    "@supperajan/types": "workspace:*",
    "@supperajan/config": "workspace:*"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
