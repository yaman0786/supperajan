{
  "name": "@supperajan/knowledge",
  "version": "0.1.0",
  "private": true,
  "description": "RAG/knowledge subsystem: ingestion pipeline, vector search, retrieval ranking",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./providers": "./src/providers/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@supperajan/types": "workspace:*",
    "@supperajan/ai": "workspace:*",
    "@supperajan/observability": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
