{
  "name": "@supperajan/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI orchestration layer: provider interfaces, prompt building, emotion inference",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./providers": "./src/providers/index.ts",
    "./orchestration": "./src/orchestration/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@supperajan/types": "workspace:*",
    "@supperajan/config": "workspace:*",
    "@supperajan/observability": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
