{
  "name": "@supperajan/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Memory subsystem: rules, interfaces, retrieval, write logic",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@supperajan/types": "workspace:*",
    "@supperajan/observability": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
